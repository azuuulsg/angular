export interface flores {
    id: number;// id tipo number para que pueda ser auto incrementableng
    img: string;
    alt: string;
    nombre: string;

    precio: number;
    descripcion: string;
}